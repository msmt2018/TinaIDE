# Deprecated workflow template.
#
# TinaIDE no longer uses the old private-repository `repository_dispatch`
# handoff flow. Do not copy this file into `.github/workflows/`.
#
# Current release builds are handled directly by:
#   .github/workflows/release.yml
#
# The current workflow builds and publishes architecture-specific APKs:
#   TinaIDE-<version>-arm64-v8a.apk
#   TinaIDE-<version>-x86_64.apk
