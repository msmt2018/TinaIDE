; Code block patterns for Java constructs

(class_declaration
  body: (_) @scope.marked)

(interface_declaration
  body: (_) @scope.marked)

(record_declaration
  body: (_) @scope.marked)

(enum_declaration
  body: (_) @scope.marked)

(annotation_type_declaration
  body: (_) @scope.marked)

(block) @scope.marked

(switch_block) @scope.marked

(array_initializer) @scope.marked
